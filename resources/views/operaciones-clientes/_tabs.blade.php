<nav class="mb-6 flex max-w-full flex-nowrap gap-2 overflow-x-auto rounded-2xl border border-slate-200 bg-white p-2 shadow-sm" aria-label="Apartados y envíos">
    <a href="{{ route('operaciones-clientes.index') }}" class="shrink-0 rounded-xl px-4 py-2.5 text-sm font-semibold transition {{ request()->routeIs('operaciones-clientes.*') ? 'bg-slate-900 text-white shadow' : 'text-slate-600 hover:bg-slate-100' }}">Resumen</a>
    @if(auth()->user()->isAdmin() || auth()->user()->hasPermission('apartados.view'))<a href="{{ route('apartados.index') }}" class="shrink-0 rounded-xl px-4 py-2.5 text-sm font-semibold transition {{ request()->routeIs('apartados.*') ? 'bg-indigo-600 text-white shadow' : 'text-slate-600 hover:bg-indigo-50 hover:text-indigo-700' }}">Apartados</a>@endif
    @if(auth()->user()->isAdmin() || auth()->user()->hasPermission('envios.view'))<a href="{{ route('envios.index') }}" class="shrink-0 rounded-xl px-4 py-2.5 text-sm font-semibold transition {{ request()->routeIs('envios.*') ? 'bg-sky-600 text-white shadow' : 'text-slate-600 hover:bg-sky-50 hover:text-sky-700' }}">Envíos</a>@endif
</nav>
